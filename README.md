# claude-code-via-cursor

A VS Code extension for Cursor that puts a chat interface around the `claude` (Claude Code) CLI. Prompts route through your locally-authenticated Claude Code session, so calls bill against your Claude subscription's interactive bucket instead of Cursor's API tokens.

Personal project. Not distributed. Built and installed locally as a `.vsix`.
